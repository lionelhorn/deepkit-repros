## Run on linux based systems or using wsl on windows.

### On wsl
* Install npm via npm and update npm
    ```
    nvm install --lts
    npm -g update npm
    ```
* Check node and npm versions
    ```
    node -v
    ```

